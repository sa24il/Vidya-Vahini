<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vidhyavahini">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VidyaVahini">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.VidyaVahini.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.splash.SplashActivity" />
        <activity android:name=".ui.onboarding.OnboardingActivity" />
        <activity
            android:name=".ui.home.HomeActivity"
            android:theme="@style/Theme.VidyaVahini.NoActionBar" />
        <activity android:name=".ui.routes.RoutesActivity"
            android:exported="false" />
        <activity android:name=".ui.alerts.AlertsActivity" />
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:theme="@style/Theme.VidyaVahini.NoActionBar" />
        <activity android:name=".ui.delay.DelayAdvisorActivity" />

    </application>

</manifest>
